package co.com.rapicredit.tasks.onboardingRP;

import net.serenitybdd.screenplay.Actor;
import net.serenitybdd.screenplay.Performable;
import net.serenitybdd.screenplay.Task;
import net.serenitybdd.screenplay.actions.Click;
import net.serenitybdd.screenplay.actions.Enter;
import net.serenitybdd.screenplay.actions.Scroll;
import net.serenitybdd.screenplay.questions.Text;
import org.openqa.selenium.By;

import static co.com.rapicredit.userInterface.onboardingRP.Firma.*;
import static net.serenitybdd.screenplay.Tasks.instrumented;

public class FirmarSolicitud {

/*
    @Override
    public <T extends Actor> void performAs(T actor) {

        String numeroGenerado = ExtraerNumero.obtenerNumero(actor);


        actor.attemptsTo(
                /*Enter.theValue(String.valueOf(numero)).into(PRIMER_CASILLA),
                Enter.theValue(String.valueOf(numero)).into(SEGUNDA_CASILLA ),
                Enter.theValue(String.valueOf(numero)).into(TERCER_CASILLA),
                Enter.theValue(String.valueOf(numero)).into(CUARTA_CASILLA),

                Scroll.to(PRIMER_CASILLA),
                Enter.theValue(numeroGenerado).into(PRIMER_CASILLA),

                Scroll.to(ACEPTAR_TODAS_LAS_OPCIONES),
                Click.on(ACEPTAR_TODAS_LAS_OPCIONES),

                Scroll.to(BOTON_ACEPTAR),
                Click.on(BOTON_ACEPTAR)
        );
    }
    public static FirmarSolicitud firmarSolicitud(){
        return instrumented(FirmarSolicitud.class);
    }
    */
}
